import { Link } from "react-router-dom";
import logo from "../assets/Tesla-logo.png";
import "./Header.css";

const Header = () => {
  return (
    <div className="Header">
      <button className="menu-selector">Menu</button>
    </div>
  );
};

export default Header;
